using Interview20201204.Models;
using Newtonsoft.Json;
using System;
using System.Collections.Generic;
using System.Diagnostics;
using System.Diagnostics.CodeAnalysis;
using System.Linq;

namespace Interview20201204
{
    class Program
    {
        static void Main(string[] args)
        {

            //List<Shift> groupedShifts = VenueShuffle_v2(input);

            //groupedShifts.lenth = input.length

            // given ordered input, expect randomized output, unordered.
            // give test a and b next to each other  based on the data: 
            // venueShuffle(shifts) => [a, b, c, d, e]
            // venueShuffle(shifts) => [c, d, a, b, e]
            // x,y,z if they all have same company and same location in the output they should be grouped together.

            //var l1 = new List<int>();
            //var l2 = new List<int>();
            //var isEqualSequence = l1.SequenceEqual(l2);
        }
    }
}
